package com.isep.sensordashboard

import org.junit.Test
import org.junit.Assert.*

/**
 * Example unit test.
 * 
 * TODO(Bonus): Add a small unit test on a helper (e.g. computing an average from readings).
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}

